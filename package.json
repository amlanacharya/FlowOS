{
  "name": "flowos-tooling",
  "private": true,
  "scripts": {
    "dev": "npm run dev:backend",
    "dev:backend": "npm run run:backend:local",
    "dev:frontend": "npm --prefix web run dev",
    "build": "npm run build:backend",
    "build:backend": "npm run build:backend:local",
    "build:backend:local": ".\\venv\\Scripts\\python.exe verify_build.py",
    "build:backend:docker": "docker compose build app",
    "run:backend:local": ".\\venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001",
    "run:backend:local:reload": ".\\venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload",
    "build:frontend": "npm --prefix web run build"
  }
}
